@page "/"
@using Data
@inject LoggedIn login
@inject NavigationManager NavigationManager
<h1>Prest Energy!</h1>
<div>
    @if (login.loggedIn)
    {
        text = "Welcome!";
    }
    else
    {
        text = "Login to access the system.";
    }
</div>
<h4>@text</h4>

@code{
    private string text = "";
}